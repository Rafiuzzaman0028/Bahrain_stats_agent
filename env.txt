# ===============================
# Bahrain Statistical AI Agent
# Environment Configuration File
# ===============================

# Application environment
ENVIRONMENT=development

# Enable or disable debug logs in development
DEBUG=true

# -------------------------------
# Webhook Receiver Configuration
# -------------------------------

# Host & port for the webhook server
# In production, set WEBHOOK_HOST=0.0.0.0
WEBHOOK_HOST=127.0.0.1
WEBHOOK_PORT=5000

# Should the webhook enforce stricter validation?
STRICT_WEBHOOK=true

# -------------------------------
# Fetch & Ingest System Settings
# -------------------------------

# Max download size per file (bytes) — 50 MB default
MAX_DOWNLOAD_BYTES=52428800

# Log rotation frequency (use: midnight, hourly, weekly)
LOG_ROTATION=midnight

# -------------------------------
# Local development utilities
# -------------------------------

# Local test file (you uploaded earlier)
# Useful for testing the fetch_incoming pipeline without real APIs
TEST_FILE_URL=file:///mnt/data/Untitled25.ipynb
