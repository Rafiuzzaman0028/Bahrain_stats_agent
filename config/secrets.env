# Secrets and environment variables for Bahrain_Statistical_AI_Agent
# Do NOT commit this file to version control. Keep it private.

# Webhook authentication secret (required by webhook_receiver.py)
# Set this to a long random string and give the same value to any client
# that will call your webhook (X-API-KEY header or ?api_key=...)
WEBHOOK_SECRET=be7b8b0e3f4141489f2c68848a3242c1
# Optional: general API key(s) used by fetch scripts or other integrations
# Example: to access protected data provider APIs
# You can define per-endpoint keys in endpoints.json and/or put global tokens here
API_TOKEN=replace_me_if_needed

# OpenAI API key (if you use OpenAI or other LLM services)
# If you later integrate an LLM, put its key here and load via os.getenv('OPENAI_API_KEY')
OPENAI_API_KEY=sk-REPLACE-WITH-YOUR-OPENAI-KEY

# Example third-party service token (placeholder)
THIRD_PARTY_SERVICE_TOKEN=replace_me_if_needed

# Local test file URL (developer convenience) - DO NOT INCLUDE IN PRODUCTION
# This references the file you uploaded earlier. The fetcher supports file:// paths
# and will copy this into data/incoming/ during local tests.
# We include it so you can run the fetch script during development without remote endpoints.
TEST_FILE_URL=file:///mnt/data/Untitled25.ipynb

# Optional: set environment for production or development
ENVIRONMENT=development

# Extra config flags (optional)
# If you run webhook on a public host, set this to 'true' to enable stricter checks
STRICT_WEBHOOK=true
