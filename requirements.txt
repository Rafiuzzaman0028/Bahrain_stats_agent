# Core utilities
python-dotenv==1.0.1
requests==2.31.0
pandas==2.3.3

# Web server (Webhook)
Flask==3.0.2
Werkzeug==3.0.1

# File watching / auto-ingestion (if used)
watchdog==4.0.0

# For robust CSV / data handling
numpy==1.26.4

# Optional: if using OpenAI or any LLM
openai==1.30.1

# If you use Gradio for chatbot UI later
gradio==4.19.2

# For general utilities
regex==2023.12.25

# For parsing dates and time zones
python-dateutil==2.9.0.post0

# For JSON & YAML handling
pyyaml==6.0.1
